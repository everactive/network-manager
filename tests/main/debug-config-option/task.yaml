summary: Test network-manager snap debug configuration

execute: |
    . $TESTSLIB/utilities.sh
    
    test "$(snap get network-manager debug)" = "false"
    ! journalctl --no-pager -u snap.network-manager.networkmanager.service | grep -Pzq "<debug>"

    snap set network-manager debug=true
    wait_for_network_manager
    test "$(snap get network-manager debug)" = "true"
    test -e /var/snap/network-manager/current/conf.d/.debug_enabled
    ps -ef | grep -z "NetworkManager.*\-\-debug" | grep -v grep
    journalctl --no-pager -u snap.network-manager.networkmanager.service | grep -Pzq "<debug>"

    journalctl --rotate
    sleep .1
    journalctl --vacuum-time=1ms
    dmesg -c > /dev/null

    snap set network-manager debug=false
    wait_for_network_manager
    test "$(snap get network-manager debug)" = "false"
    test ! -e /var/snap/network-manager/current/.debug_enabled
    ! journalctl --no-pager -u snap.network-manager.networkmanager.service | grep -Pzq "<debug>"
